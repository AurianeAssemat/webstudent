<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
		
		LISTE DES PROFESSEURS</br></br>
		<table>
		<tr><td>Nom</td><td>Prenom</td></tr>
		{% for p in pProfesseurs %}
		<tr>
			<td><a href="{{ path('professeurConsulter', { 'id': p.id }) }}">{{ p.nom }}</a></td>
			<td>{{ p.prenom }}</td>
	{% else %}
		<tr>
		<td>Aucun professeur n'a été trouvé.</td>
		</tr>
	{% endfor %}
</table>
	
	<a href="{{ path('professeurAjouter') }}">Ajouter un pofesseur</a> </br>
	
	<a href="{{ path('accueil') }}">Retour à l'accueil</a>
	 
    </body>
</html>