<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
		
		LISTE DES STAGES</br></br>
		<table>
		{% for s in pStages %}
		<tr>
			<td><a href="{{ path('stageConsulter', { 'id': s.id }) }}">{{ s.id }}</a></td>
			<td>{{ s.objet }}</a></td>
			<td>{{ s.dateDebut|date("d/m/Y") }}</td>
			<td>{{ s.dateFin|date("d/m/Y") }}</td>
	{% else %}
		<tr>
		<td>Aucun stage n'a été trouvé.</td>
		</tr>
	{% endfor %}
</table>
	
	
	<a href="{{ path('stageAjouter') }}">Ajouter un stage</a> </br>
	
	<a href="{{ path('accueil') }}">Retour à l'accueil</a>
	 
    </body>
</html>