<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
		
		LISTE DES TUTEURS</br></br>
		<table>
		{% for t in pTuteurs %}
		<tr>
			<td><a href="{{ path('tuteurConsulter', { 'id': t.id }) }}">{{ t.id }}</a></td>
			<td>{{ t.nom }}</a></td>
			<td>{{ t.prenom }}</a></td>
	{% else %}
		<tr>
		<td>Aucun tuteur n'a été trouvé.</td>
		</tr>
	{% endfor %}
</table>
	
	
	<a href="{{ path('tuteurAjouter') }}">Ajouter un tuteur</a> </br>
	
	<a href="{{ path('accueil') }}">Retour à l'accueil</a>
	 
    </body>
</html>